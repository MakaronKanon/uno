package CardModel.SpecialCards;

import CardModel.ActionCard;
import CardModel.WildCard;

public class Draw4xCard extends WildCard implements ActionCard {
    public Draw4xCard(String cardValue) {
        super(cardValue);
    }
}
